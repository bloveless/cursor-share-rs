apiVersion: v1
kind: Service
metadata:
  name: cursor-share
  namespace: cursor-share
  labels:
    app: cursor-share
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: cursor-share

